<?xml version="1.0" encoding="utf-8"?>
<resources>
    <style name="Theme.MiniMusic" parent="android:style/Theme.Material.Light.NoActionBar">
        <item name="android:windowNoTitle">true</item>
        <item name="android:windowBackground">@color/window_background</item>
    </style>

    <!-- System splash theme: neutral field plus the concentric target mark. -->
    <style name="Theme.MiniMusic.Starting" parent="Theme.SplashScreen">
        <item name="windowSplashScreenBackground">@color/splash_background</item>
        <item name="windowSplashScreenAnimatedIcon">@drawable/ic_launcher_foreground</item>
        <item name="postSplashScreenTheme">@style/Theme.MiniMusic</item>
    </style>
</resources>
